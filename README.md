# ADD — getadd.dev deployment branch

This branch is a deployment artifact, auto-built from `website/` on `main`.

**Do not edit here directly.** Source is at https://github.com/MountainUnicorn/add/tree/main/website.

Served via GitHub Pages at https://getadd.dev

Rebuilt manually while account-level GitHub Actions is temporarily paused; once Actions returns, the build reverts to `.github/workflows/pages.yml`.
